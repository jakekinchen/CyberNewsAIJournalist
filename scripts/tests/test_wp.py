from dotenv import load_dotenv; import os; import base64; import httpx; load_dotenv(); wp_username = os.getenv("WP_ADMIN_USERNAME"); wp_app_password = os.getenv("WP_APPLICATION_PASSWORD"); auth_token = base64.b64encode(f"{wp_username}:{wp_app_password}".encode()).decode(); headers = {"Authorization": f"Basic {auth_token}"}; response = httpx.get("https://cybernow.info/wp-json/wp/v2/categories", headers=headers); print(f"Status code: {response.status_code}"); print("Categories:"); [print(f"- {category[\"name\"]} (ID: {category[\"id\"]})") for category in response.json()]
